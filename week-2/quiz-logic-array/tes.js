function targetTerdekat(arr) {
    let indexO = arr.indexOf('o');
    let indexX = arr.indexOf('x');
    let hasil;

    for(let i = 0;i < arr.length;i++){
        let hasil = arr.indexOf('x');
        console.log(hasil);
    }
} 
  console.log(targetTerdekat(['x', ' ', ' ', ' ', 'x', 'x', 'o', ' '])); // 1
